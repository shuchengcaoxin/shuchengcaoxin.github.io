# 如何更新我的网站（编辑式版本）

> 2026-09-02 更新。首页采用纯静态 HTML：没有主题、没有编译，也不需要安装依赖。

## 先记住这张地图

| 想改什么 | 文件 |
|---|---|
| 首页文字、布局、颜色 | `index.html` |
| 头像 | `assets/img/profile.jpg` |
| 英文 CV | `assets/pdf/CV_EN.pdf` |
| Blog 首页 | `blog/index.html` |
| OpenCausal 技术文章 | `blog/what-the-agent-gets-wrong/index.html` |

网页由 GitHub Pages 托管。只有把本地 commit **push 到 GitHub 的 `main` 分支后**，公开网站才会变化；本地编辑和本地 commit 都不会自动公开。

---

## 一、先在本地预览（推荐）

在 **Windows PowerShell** 里运行：

```powershell
cd D:\Work_Destiny\Internship_CABS\shuchengcaoxin.github.io
python -m http.server 8000 --bind 127.0.0.1
```

然后浏览器打开：

```text
http://127.0.0.1:8000/
```

- 保存 `index.html` 后，刷新浏览器就能看到新版本。
- PowerShell 看起来一直被占用是正常的：它正在运行本地网页服务。
- 看完后按 **`Ctrl+C`** 停止服务。

---

## 二、加一条近期动态

1. 打开 `index.html`。
2. 按 `Ctrl+F` 搜索 `Recent updates`。
3. 在 `<ul class="updates" ...>` 下面复制一整行 `<li>...</li>`，放到最上面。
4. 修改机器日期、人读日期和内容。

示例：

```html
<li>
  <time datetime="2026-10-20">Oct 2026</time>
  <span>Presented our work at <strong>ASHG 2026</strong>.</span>
</li>
```

- `datetime="2026-10-20"` 给搜索引擎和辅助工具读取，格式是 `年-月-日`。
- `Oct 2026` 是页面上实际显示的文字。
- `<strong>...</strong>` 只给最值得扫到的事实加粗，不要一行加粗三四处。

首页现在只保留最近几条动态，演讲的完整清单放在 CV，不在首页重复一遍。

---

## 三、更新“我现在在做什么”

按 `Ctrl+F` 搜索：

```text
What is on my desk
```

下面有两个 `<article class="now-item"> ... </article>`：

1. PhD thesis；
2. CausalSentinel / OpenCausal。

每项按这个顺序写：

```text
时间/身份 → 项目名称 → 你现在具体在做什么 → 一个可核验的规模或结果
```

例如可以写“790,000+ participants”“two independent proteomic platforms”，不要改成“cutting-edge”“revolutionary”“high-impact”这类自我评价。

---

## 四、换头像和 CV

### 换头像

把新照片命名为 `profile.jpg`，覆盖：

```text
assets/img/profile.jpg
```

建议宽度 600–1,000 像素、文件小于 300 KB。页面会自动裁成横向较窄的人像框。

### 换英文 CV

把新 PDF 命名为 `CV_EN.pdf`，覆盖：

```text
assets/pdf/CV_EN.pdf
```

文件名保持不变，页面里的所有 CV 链接就不用改。

---

## 五、这版“去 AI 味”的四条写作规则

1. **先写事实，再写意义。** 例如先写 12 家医院、1,000+ CT，再解释为什么要做跨医院测试。
2. **一页只留一两句有力度的话。** 不要每段都用“不是 X，而是 Y”“I do X, but...”的反差金句。
3. **少列关键词，多展示工作过程。** MR、colocalization、R、Python 在页面里出现一次就够；真实诊断记录、论文、代码或失败案例更可信。
4. **不要把首页写成广告。** 不用徽章墙、技能 logo 墙、GitHub stats、进度条、发光渐变或三张并排的“价值主张”卡。
5. **关键求职状态不要写成脚注。** 毕业时间和可入职窗口属于招聘者的首屏决策信息，应该放在研究定位之后、个人简介之前，并用克制但清楚的状态栏呈现。

判断一句话是否像你，可以问自己：**我会不会在组会里这样说？** 如果不会，就改成你真的会说的版本。

---

## 六、改完后的安全检查

仍然在 **Windows PowerShell**、网站仓库目录里运行：

```powershell
git status -sb
git diff --check
git diff --stat
```

- `git status -sb`：看自己在哪条分支、改了哪些文件。
- `git diff --check`：检查多余空格等容易漏掉的小问题；没有输出通常就是通过。
- `git diff --stat`：只看本次改动涉及哪些文件和多少行。

本地确认后再 commit；**push 会立刻影响公开网站，所以 push 前再看一次预览。**

---

## 七、Google 搜不到姓名时，先分清“收录”和“排名”

- **收录（indexing）**：Google 有没有把网页放进数据库。
- **排名（ranking）**：网页已经在数据库里，但搜某个词时排在第几位。

不要只凭 `site:` 搜索下结论；它显示的结果并不完整。最终以 **Google Search Console 的 URL Inspection** 为准。

### 这次查到的实际情况（2026-09-02）

当前线上首页、`robots.txt`、`sitemap.xml` 都返回 200；页面没有 `noindex`，canonical 也已指向自己。因此，问题不是“代码把 Google 挡住了”。

真正的薄弱处是：

1. 网站 2026-05-24 初次部署时短暂保留过模板的 `You R. Name`、错误 canonical 和大量示例 URL；干净的个人站其实到 2026-08-03 才稳定下来。
2. GitHub、Google Scholar、ORCID 等权威个人资料还没有充分反向链接到官网。
3. `Shucheng Cao` 存在同名竞争，而 `Shucheng Cao` / `Bangli Cao` 两套署名此前没有在所有页面上统一关联。

### 已在代码里补好的信号

- 页面标题和正文同时出现连续的 `Shucheng Cao` 与 `Bangli Cao`。
- 首页结构化数据改为 `WebSite + ProfilePage + Person`，并用固定 `#person` ID 关联人物。
- `sameAs` 加入 ORCID、Scholar、GitHub 和 LinkedIn。
- 博客文章加入 `Article` 结构化数据，并把作者连回同一个 `#person`。
- sitemap 加入准确的 `lastmod`；旧页面指向的失效锚点也已修复。

这些信号帮助搜索引擎理解“这些名字和页面是同一个人”，但**不能保证立刻收录或排到第一**。

### 仍需在账号网页上做的一次性操作

1. 打开 Google Search Console，添加 **URL-prefix property**：

   ```text
   https://shuchengcaoxin.github.io/
   ```

2. 用 HTML tag 或 HTML file 完成验证。
3. 在 Sitemaps 提交：

   ```text
   https://shuchengcaoxin.github.io/sitemap.xml
   ```

4. 在 URL Inspection 检查首页，点 **Test live URL**，再点 **Request indexing**。
5. Google Scholar 的 Homepage、ORCID 的 Websites & social links、LinkedIn Contact info 都补上个人网站。

提交后按“天到周”观察，不要一天反复点很多次；重复提交不会让抓取更快。
