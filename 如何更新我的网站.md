# 如何更新我的网站

你的整个网站现在就是**一个文件**：`index.html`。没有编译、没有 CI、没有主题。
改完提交，10–60 秒后线上就变了。改错了也不会让网站挂掉，最坏就是某一行显示得难看，而且随时能在 GitHub 的提交历史里一键回退。

---

## 一、加一条 News（最常做的事，约 45 秒）

1. 打开 https://github.com/shuchengcaoxin/shuchengcaoxin.github.io/edit/main/index.html
   （建议直接把这个网址存成浏览器书签，叫「改我的网站」）
2. 按 `Ctrl+F` 搜 `ADD A NEW ITEM`，会跳到 News 区。你会看到这样一段注释：

   ```
   <!-- ============================================================
        NEWS — ADD A NEW ITEM AT THE TOP OF THIS LIST.
        ============================================================ -->
   ```

3. 把注释**下面**紧接着的那一整块 `<li> ... </li>` 复制一份，粘贴成新的第一条。
4. 只改两个地方：日期和那句话。**其它一个字都不用动。**
5. 拉到页面底部，点绿色的 **Commit changes**。

一条 news 长这样：

```html
<li>
  <time datetime="2026-10-20">Oct 2026</time>
  <div class="body"><span class="tag">Talk</span>在 <strong>ASHG 2026</strong> 做了报告。</div>
</li>
```

- `datetime="2026-10-20"` 是给机器读的，格式必须是 `年-月-日`。
- `Oct 2026` 是给人看的，写什么都行。
- `<span class="tag">…</span>` 是那个小标签，可以写 `Paper` / `Talk` / `Award` / `Code`，也可以整个删掉不要。
- `<strong>…</strong>` 包起来的字会加粗。

**第 6 条以后的 news 会自动折叠**到「Show earlier news」按钮里，所以你一直往上加就行，页面不会越来越长。

---

## 二、换照片

把照片存成 **`profile.jpg`**，上传到仓库的 `assets/img/` 文件夹里，覆盖即可，代码一个字不用改。

- 上传方法：进 `assets/img/` 文件夹 → 右上角 **Add file → Upload files** → 拖进去 → Commit。
- **先把图压到 600 像素宽、100KB 以内**（微信发一次再存下来就差不多）。手机直出的 3MB 大图会明显拖慢加载，是这类页面跑分最常见的失分项。
- 没上传照片之前，页面显示的是你名字缩写的方块，不难看，不用急。

## 三、换 CV

把新 PDF 传到 `assets/pdf/`，文件名保持 **`CV_EN.pdf`**，直接覆盖。链接不用改。

---

## 四、三条要记住的事

**1. News 停更比没有 News 更糟。**
调研了 30 多个真实学者主页，停更的 news 是最刺眼的负面信号——有的停在 2020 年，整页因此显得"这人早就没动静了"。
你的产能是每周 20–30 分钟。**只要保证一个月更一条，就一定值得留着。**做不到就告诉我，我把这一节改成别的形式。

**2. 找到工作之后，第一件事是把求职那句话删掉。**
真实案例：有人页面上同时挂着"我在找工作"和"我已经入职 Meta 了"，自相矛盾。

**3. 不要买自定义域名。**
调研里 2025 年拿"最佳学术个人主页"的那个站，现在域名已经过期、跳到了广告停放页。你的网址要写进论文的 Data Availability 声明，`github.io` 是免费且不会过期的那个选择。

---

## 五、上线后要做的两件一次性的事

**1. 刷新 LinkedIn 的预览缓存。**
LinkedIn 会把链接预览缓存约 7 天。改版后第一次贴之前，先把网址丢进 https://www.linkedin.com/post-inspector/ 点一下，否则接下来一周你贴出去的都是旧的空白卡片。

**2. 让 Google 重新抓取。**
去 https://search.google.com/search-console 加一个 URL prefix 属性（`https://shuchengcaoxin.github.io/`），用 **HTML meta tag** 方式验证（github.io 无法用 DNS 验证），然后 URL Inspection → Request Indexing。
