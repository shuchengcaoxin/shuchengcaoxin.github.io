# 如何更新我的网站

> 2026-09-11 更新。网站是纯静态 HTML：没有主题、没有编译，也不需要安装依赖。

## 先记住这张地图

| 想改什么 | 文件 |
|---|---|
| 首页结构和文字 | `index.html` |
| 全站主要颜色、字体、间距 | `assets/css/site.css` |
| News | `index.html` 里的 `<section id="news">` |
| Blog 列表 | `blog/index.html` |
| 某篇 Blog 正文 | `blog/<文章短名>/index.html` |
| 头像 | `assets/img/profile.jpg` |
| 英文 CV | `assets/pdf/CV_EN.pdf` |
| 搜索引擎页面清单 | `sitemap.xml` |

首页的固定阅读顺序是：

```text
Summary → News → Selected Work → Publications → Blog → Contact
```

不要再把 News 和 Blog 合成 `Notes`。

网页由 GitHub Pages 托管。只有把本地 commit **push 到 GitHub 的 `main` 分支后**，公开网站才会变化；本地编辑和本地 commit 都不会自动公开。

---

## 一、先在本地预览

在 **Windows PowerShell** 里运行：

```powershell
cd D:\Work_Destiny\Internship_CABS\shuchengcaoxin.github.io
python -m http.server 8000 --bind 127.0.0.1
```

然后在浏览器打开：

```text
http://127.0.0.1:8000/
```

- 保存文件后，刷新浏览器就能看到新版本。
- PowerShell 一直显示被占用是正常的：它正在运行本地网页服务。
- 看完后在 PowerShell 按 **`Ctrl+C`** 停止服务。

---

## 二、加一条 News

1. 打开 `index.html`。
2. 按 `Ctrl+F` 搜索 `id="news"`。
3. 在 `<ol class="news-list">` 下面复制一整段 `<li class="news-item">...</li>`，放到最上面。
4. 修改机器日期、显示日期、类型和正文。

示例：

```html
<li class="news-item">
  <time class="news-date" datetime="2026-10-20">20 Oct 2026</time>
  <span class="news-kind">Talk</span>
  <span class="news-copy">Presented our work at <strong>ASHG 2026</strong>.</span>
</li>
```

- `datetime="2026-10-20"` 给搜索引擎和辅助工具读取，格式是 `年-月-日`。
- 日期不确定到哪一天时，可以写 `datetime="2026-10"`，页面上显示 `Oct 2026`。
- 类型可用 `Blog`、`Paper`、`Talk`、`Poster`、`Award`、`Teaching`。
- 首页默认露出最近六条；更早的内容放进 `<details class="news-archive">`。

---

## 三、发布一篇 Blog：必须同步四处

一篇 Blog 不是只加正文。每次发布都要同时完成：

1. 新建正文：`blog/<文章短名>/index.html`；
2. 在 `blog/index.html` 最上面加文章卡片；
3. 在首页 News 最上面加一条**可点击的 Blog 链接**；
4. 在 `sitemap.xml` 加文章网址。

News 的 Blog 条目模板：

```html
<li class="news-item">
  <time class="news-date" datetime="2026-10-20">20 Oct 2026</time>
  <span class="news-kind blog">Blog</span>
  <span class="news-copy"><a href="/blog/example-slug/">Article title</a></span>
</li>
```

如果这篇是最新文章，还要把首页 `<section id="blog">` 里的 featured Blog 卡片换成它。Blog 页面保留全部文章；首页 Blog 区只突出最新或最重要的一篇。

### 防漏检查

在 **Windows PowerShell**、网站仓库目录里运行：

```powershell
python scripts\check_blog_news_sync.py
```

它会逐篇检查：

- Blog 列表中的文章文件是否真的存在；
- 每篇 Blog 是否在首页 News 有同一条链接；
- 每篇 Blog 是否进入 `sitemap.xml`。

全部显示 `[PASS]` 才算发布准备完成。这个脚本不会修改任何文件。

---

## 四、更新 Selected Work 或 Publications

### Selected Work

在 `index.html` 搜索 `id="work"`。每个项目保持同一个顺序：

```text
问题为什么难 → 我具体搭建了什么 → 什么检查或数字证明它可信 → 可核验链接
```

`proof-list` 只放能分别证明规模、可靠性或采用情况的两三个数字。不要把它变成技能关键词墙。

### Publications

在 `index.html` 搜索 `id="papers"`：

- 按年份从新到旧；
- 论文标题尽量链接 DOI 或正式页面；
- 准确区分 first author 与 co-author；
- 骨关节炎 Nature Communications 文章中的姓名写 `Shucheng Cao`；
- 完整记录仍以 Google Scholar 和 master CV 为准。

---

## 五、换头像和 CV

### 换头像

用新照片覆盖：

```text
assets/img/profile.jpg
```

建议宽度 600–1,000 像素、文件小于 300 KB。页面会自动裁切；替换后必须在桌面和手机宽度各看一次。

### 换英文 CV

用新 PDF 覆盖：

```text
assets/pdf/CV_EN.pdf
```

文件名保持不变，网页里的 CV 链接就不需要改。

---

## 六、写作与设计规则

1. **先写事实，再写意义。** 先写 12 家医院、1,112 张 CT，再解释为什么跨医院测试重要。
2. **首页是入口，不是简历全文。** Summary 笼统说明你是谁；细节放到 News、Work、Publications 和 Blog。
3. **News 要有时间感。** 论文、汇报、海报、教学和新 Blog 都可以进入，但按时间倒序。
4. **不要自我评价。** 少写 `cutting-edge`、`revolutionary`、`high-impact`，让问题、方法和结果自己说话。
5. **保持同一视觉语言。** 米白为底，深绿为主强调，锈红只标日期、状态和少数重点；不要加发光渐变、技能 logo 墙或 GitHub stats。
6. **Blog 不是 Notes。** 用户可见的导航、标题、页面 metadata 一律写 `Blog`；文章内部可以按内容称 case study、essay 或 technical post。

判断一句话是否像你，可以问：**我会不会在组会里这样说？** 如果不会，就改成你真的会说的版本。

---

## 七、改完后的安全检查

仍然在 **Windows PowerShell**、网站仓库目录里运行：

```powershell
python scripts\check_blog_news_sync.py
git diff --check
git status -sb
git diff --stat
```

- `check_blog_news_sync.py`：防止 Blog、News、文章文件和 sitemap 脱节；
- `git diff --check`：没有输出通常就是通过；
- `git status -sb`：确认自己在 `main`，并看清改了哪些文件；
- `git diff --stat`：快速确认改动范围。

本地预览确认后再提交：

```powershell
git add index.html blog sitemap.xml assets\css scripts README.md "如何更新我的网站.md"
git commit -m "Update homepage news and blog"
git push origin main
```

`git push` 会影响公开网站，运行前再看一次本地预览。GitHub 若要求登录，请按浏览器或 Git Credential Manager 的提示完成；命令行输入密码时看不见字符是正常的。

---

## 八、搜索引擎说明

首页、Blog 和文章都需要保留：

- 正确的 `<title>` 和 description；
- 指向自己的 canonical URL；
- `Shucheng Cao` 与 `Bangli Cao` 的一致关联；
- `sitemap.xml` 中的 URL 和 `lastmod`。

网站已在结构化数据中把 `Shucheng Cao`、`Bangli Cao`、ORCID、Scholar、GitHub 和 LinkedIn 关联为同一人。改首页时不要删掉这段 JSON-LD。
